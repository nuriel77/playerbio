apiVersion: enterprise.splunk.com/v1alpha2
kind: Standalone
metadata:
  name: s1
  namespace: splunk
spec:
  storageClassName: ""
  etcStorage: "250Mi"
  varStorage: "500Mi"
  resources:
    requests:
      memory: "500Mi"
      cpu: "500m"
    limits:
      memory: "1Gi"
      cpu: "2000m"
